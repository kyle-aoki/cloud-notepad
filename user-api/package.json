{
  "name": "user-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx jest",
    "build": "rimraf build && tsc && rimraf build/test/",
    "start:dev": "npm run build && set NODE_ENV=dev&& node build/main/server/",
    "start:stage": "npm run build && set NODE_ENV=stage&& node build/main/server/",
    "start:prod": "npm run build && set NODE_ENV=prod&& node build/main/server/",
    "dev": "set NODE_ENV=dev&& ts-node-dev src/main/server/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.23",
    "@types/morgan": "^1.9.2",
    "@types/node": "^15.6.0",
    "chalk": "^4.1.1",
    "express": "^4.17.1",
    "jest": "^26.6.3",
    "morgan": "^1.10.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.6",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4",
    "winston": "^3.3.3"
  }
}
